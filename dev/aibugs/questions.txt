### Introduction

1. When you say "missuses of the libr apis" — do you want to enumerate the specific libr API families that are most commonly misused? (e.g., r_core_cmd*, r_cons_printf, r_str_*, etc.)
2. "and scripts instead" — what scripts? Are you referring to r2pipe scripts, r2js scripts, the autogenerated * command scripts, or all of the above?
3. Should there be a brief explanation of the threat model? (e.g., attacker controls a malicious binary loaded into r2, attacker controls input via r2pipe, etc.)

### Steps — General

4. Step 1 says "Read AGENTS.md" — is this a file in the radare2 repo, or the AGENTS.md in the skills repo? Should the path be specified?
5. The numbered steps mix coding guidelines (step 1) with vulnerability categories (steps 2-3). Should these be separated into "Rules" vs "Vulnerability classes to check"?

### Command Injection via r_core_cmd

6. "The r_core_cmd apis parse special characters" — can you list which special characters are dangerous? (;, @, |, ~, `, ", etc.)
7. "which if the user or the binary loaded have control" — this sentence is incomplete. Control over what exactly? The arguments passed to r_core_cmd?
8. "Better use r_core_call functions" — what is the exact difference between r_core_cmd and r_core_call? Does r_core_call skip the command parser entirely?
9. "Prefer the use of call_at if a temporal seek is needed" — is this r_core_call_at? What's its full signature? How does it differ from doing a manual seek + call + seek-back?

### Autogenerated Scripts (the * suffix)

10. "Read the r_cons_printf calls used in those subcommands to find out any" — this sentence is cut off. Find out any what? Unsanitized format strings? Injection vectors in the generated script output?
11. Can you give a concrete example of a vulnerable * command? e.g., a command that outputs "f sym.NAME 0x1234" where NAME comes from the binary and could contain ; or backticks?
12. Should the skill also check r_cons_println and r_strbuf_appendf for the same class of issues, or only r_cons_printf?

### Single-Quote Command Syntax

13. '@0x123'x — is the @ inside the quotes significant, or is the format '<tempseek>'<command>? The example is a bit ambiguous about which part is the seek and which is the command.
14. "Also, an important hint for single quote commands" — what is the hint? This sentence seems to introduce something but doesn't complete the thought.
15. When you say single-quote commands "ignore all the special characters" — does this mean they are always safe, or are there edge cases (like the ! shell escape mentioned below)?

### Double-Quote Commands

16. The example "echo hello";"echo "world" — is this radare2 syntax or shell syntax? The quoting looks intentionally broken to demonstrate injection. Can you annotate which part is the attacker-controlled payload?
17. Are double-quote commands the same as the " prefix command in r2 (system call)? Or is this about literal double quotes inside r_core_cmd strings in C code?

### Shell Escape (!)

18. "see the sanitize for sh in this case" — what does "sanitize for sh" mean concretely? Is there an existing API like r_str_shellescape() that should be used?
19. Should the skill check for r_sys_cmd, r_sys_cmdf, popen, system() calls as well, since those also pass through the shell?

### Sanitization APIs

20. "See r_name_filter, and all the r_str_sanitize apis" — can you list the specific functions? e.g., r_str_sanitize, r_name_filter, r_name_check, r_str_shellescape, etc.?
21. "do not reduce the." — this sentence is incomplete. "Do not reduce the" what? The filtered character set? The scope of sanitization?
22. For each sanitization function, what's the intended use case? (e.g., r_name_filter is for flag names, r_str_sanitize is for console output, etc.)

### base64: Arguments

23. Which commands accept base64: arguments? Is this a general parser feature or only supported by specific commands?
24. When you say "if we really need raw data accepting any characters" — is base64: the recommended fix for cases where sanitization would break functionality?

### Missing Topics?

25. Should format string vulnerabilities (r_cons_printf(user_data) without %s) be covered as a separate category?
26. Should buffer overflows (e.g., snprintf truncation leading to logic bugs, or missing bounds checks) be in scope?
27. What about use-after-free or double-free patterns — are those in scope for this skill, or is it focused exclusively on injection?
28. Should there be guidance on how to test/confirm a vulnerability with r2 oneliners? The document mentions it's recommended but doesn't show how.
29. Should the skill know about R_LOG_* vs eprintf vs r_cons_printf distinctions for safe output?
